Hum yaha 2 variables lenge ->
global maxsum = INT_MIN;
local sum = 0;

            Now,  int maxsum = INT_MIN;
                  int sum = 0;
                      for(int i=0; i<nums.size(); i++){
                        sum += nums[i];
                        maxsum = max(maxsum,sum);

                        if(sum<0) sum = 0;
                      }
                  return maxsum;

TC : O(N)

