2 variables lo 

int maxprofit = 0 aur int minprice = INT_MAX

for(int i = 0; i<prices.size(); i++){
    minprice = min(minprice, prices[i]);
    maxprofit = max(maxprofit, prices[i] - minprice);
}

return maxprofit;


minprice minimum price nikalta rahega current element se compar karke aur maxprofit maximum profit -  TC: 0(N)